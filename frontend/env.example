# Desarrollo local: backend en localhost:8006 (o el puerto que uses)
REACT_APP_API_URL=http://localhost:8006
GENERATE_SOURCEMAP=false

# Producción (mismo servidor/nginx): dejar vacío para peticiones al mismo origen
# REACT_APP_API_URL=
