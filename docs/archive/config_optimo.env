# ===========================================
# CONFIGURACIÓN ÓPTIMA: ECONÓMICA + EFICIENTE + PRECISA
# Para más información ver README.md y README_DEV.md
# ===========================================

APP_NAME=Document Extractor API - Configuración Óptima
DEBUG=True
HOST=0.0.0.0
PORT=8005

# 🏆 SUPABASE - Ganador en Bases de Datos (36/40 puntos)
# ✅ 500MB + 2GB bandwidth GRATIS PERMANENTE
# ✅ PostgreSQL completo + Dashboard + Auth + Storage
# Registro: https://supabase.com/
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROYECTO].supabase.co:5432/postgres
DATABASE_URL_FALLBACK=sqlite:///./data/documents.db

# 🏆 UPSTASH - Ganador en Cache (36/40 puntos) 
# ✅ 10,000 requests/día GRATIS + REST API + Edge global
# Registro: https://upstash.com/
REDIS_URL=rediss://:[PASSWORD]@[HOST].upstash.io:6380
REDIS_HOST=[HOST].upstash.io
REDIS_PORT=6380
REDIS_DB=0

# 🏆 GOOGLE VISION - Ganador en OCR (36/40 puntos)
# ✅ 1,000 requests/mes GRATIS + $300 créditos + 95-98% precisión
# Registro: https://console.cloud.google.com/
GOOGLE_APPLICATION_CREDENTIALS=./google-service-account.json
GOOGLE_VISION_DAILY_LIMIT=33  # 1000/mes = ~33/día

# 🏆 OPENAI GPT-3.5 - Ganador en LLM (34/40 puntos)
# ✅ $5 USD gratis (~2,500 extracciones) + $0.002/request después
# Registro: https://platform.openai.com/
OPENAI_API_KEY=sk-proj-[TU_KEY]
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_MAX_TOKENS=800
OPENAI_TEMPERATURE=0

# 🏆 CLOUDFLARE R2 - Ganador en Storage (36/40 puntos)
# ✅ 10GB + 1M requests/mes GRATIS + CDN global + Sin egress
# Registro: https://cloudflare.com/products/r2/
# R2_ENDPOINT=https://[ACCOUNT_ID].r2.cloudflarestorage.com
# R2_ACCESS_KEY_ID=[ACCESS_KEY]
# R2_SECRET_ACCESS_KEY=[SECRET_KEY]
# R2_BUCKET_NAME=document-extractor

# Configuración optimizada para máximo rendimiento gratuito
SECRET_KEY=optimized-secret-key-change-in-production-2024
UPLOAD_DIR=uploads
OUTPUT_DIR=outputs

# Límites optimizados para tier gratuito
TESSERACT_CONFIDENCE_THRESHOLD=0.75
RATE_LIMIT_PER_MINUTE=20  # Conservador para APIs gratuitas
RATE_LIMIT_BURST=5
RQ_WORKER_TIMEOUT=300

# Configuración de fallbacks inteligentes
# Si Google Vision falla → Tesseract
# Si OpenAI falla → spaCy + regex
# Si Upstash falla → Sin cache (funciona igual)
# Si Supabase falla → SQLite local

















